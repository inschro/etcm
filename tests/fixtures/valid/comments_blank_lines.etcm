# Leading comment

spec ServiceSettings:
  # Field comment
  environment: str [in ["dev", "prod"]] # trailing field comment

  timeout_seconds: float = 30.0 [>0.0]
  label: str = "prod#primary"

impl prod:
  environment: "prod" # trailing assignment comment

  timeout_seconds: 10.0
  label: "service#prod"
